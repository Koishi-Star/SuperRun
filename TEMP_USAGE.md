# Temporary Usage Note

The temporary usage guide has been folded into [`README.md`](./README.md).

Use these commands for the current build:

```bash
npm install
npm run dev -- "hello"
npm run dev --
```
